<h2>Membres de l'équipe</h2>

{% from "./modules/people.njk" import people %}

{% for teammate in collections.teammates %}

	{{ people(teammate.data.name, teammate.data.role, teammate.data.email, teammate.data.site) }}

{% endfor %}
